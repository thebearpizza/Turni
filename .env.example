NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_EMAIL=mailto:admin@turni.app

# URL pubblico dell'app (usato nelle email e nei link di approvazione)
NEXT_PUBLIC_APP_URL=https://your-vercel-deployment.vercel.app

# Email notifications (Resend — https://resend.com)
RESEND_API_KEY=re_your_resend_api_key
RESEND_FROM_EMAIL=noreply@yourdomain.com
ADMIN_EMAIL=flavianoconiglio94@gmail.com

# Google Generative AI (Gemini) — assistente Telegram, controllo duplicati
# spese Cassa, matching articoli fatture. Senza questa chiave le
# funzionalità AI si disattivano in automatico (fallback a comportamento
# non-AI).
GOOGLE_GENERATIVE_AI_API_KEY=your-google-generative-ai-api-key
GEMINI_MODEL=gemini-2.5-flash
GEMINI_FALLBACK_MODEL=gemini-2.5-flash-lite

# Estrazione OCR fatture — coppia separata, così da poterla alzare
# indipendentemente dal resto. gemini-2.5-pro sarebbe più accurato sui
# nomi articolo, ma richiede un piano Gemini con quota su Pro: senza,
# risponde RESOURCE_EXHAUSTED e si perdono solo secondi prima di
# ripiegare comunque su Flash.
GEMINI_MODEL_ESTRAZIONE=gemini-2.5-flash
GEMINI_FALLBACK_MODEL_ESTRAZIONE=gemini-2.5-flash-lite

# ── Prenotazioni: ingresso delle notifiche dei libri visite ──────────
# La tab Prenotazioni (solo manager) aggrega TheFork e Restoo. Senza
# questa variabile funziona comunque, ma solo con inserimento manuale e
# import da file — nessuna prenotazione entra da sola.
#
# Via d'ingresso automatica: webhook di posta in entrata (CloudMailin).
# Non serve un dominio: CloudMailin (cloudmailin.com, piano gratuito
# sufficiente) dà un indirizzo pronto all'uso tipo
# xxxxxxxxxxxx@cloudmailin.net. Configurazione:
#  1. Crea un account CloudMailin, aggiungi un indirizzo.
#  2. "HTTP Post Format" → JSON.
#  3. "Target URL" → https://<dominio-app>/api/cassa/prenotazioni/webhook?secret=<PRENOTAZIONI_WEBHOOK_SECRET>
#  4. Dentro TheFork Manager e Restoo, imposta quell'indirizzo email come
#     destinatario delle notifiche di prenotazione (nuova/modifica/disdetta).
# Il segreto è un valore a scelta, generato da te (es. una stringa
# casuale lunga): autentica le chiamate al webhook, che altrimenti
# chiunque conoscesse l'URL potrebbe invocare.
PRENOTAZIONI_WEBHOOK_SECRET=your-webhook-shared-secret

# ── Telegram: bot di servizio (link account, promemoria, assistente AI) ──
# Configurazione, una volta sola:
#  1. Parla con @BotFather su Telegram → /newbot → copia il token.
#  2. TELEGRAM_WEBHOOK_SECRET è un valore a scelta, generato da te: lo
#     stesso passato a setWebhook, verifica che gli update arrivino
#     davvero da Telegram.
#  3. Dopo il deploy, registra il webhook chiamando una volta
#     GET /api/telegram/setup (riservato al manager).
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_WEBHOOK_SECRET=your-telegram-webhook-shared-secret
